@model IEnumerable<BookStore.Entities.ValueAtt>

@*<div class="form-group">*@
        <select id="values" name="values" class="form-control">
            @foreach (var item in Model)
            {
                <option value="@item.Id">@item.ValueText</option>
            }
        </select>
@*</div>*@
@*<div class="form-group">
    @Html.Label("AttributeValue", htmlAttributes: new { @class = "control-label col-md-2" })
    <div class="col-md-10">
        @Html.DropDownList("Values", ViewBag.Values as SelectList, htmlAttributes: new { @class = "form-control", id = "values" })
    </div>
</div>*@